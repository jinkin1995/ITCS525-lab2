[project]
name = "calculator-server"
version = "0.1.0"
description = "Mini Calculator API for ITCS525 Lecture 2"
requires-python = ">=3.11"
dependencies = [
    "asteval>=1.0",
    "fastapi>=0.110",
    "uvicorn>=0.29",
]

[dependency-groups]
dev = [
    "httpx>=0.27",
    "pytest>=8.0",
]

[tool.uv]
package = false
